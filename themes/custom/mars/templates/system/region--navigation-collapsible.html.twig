{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div id="regionNavigationCollapsible" {{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
